I will apply a series of edits to `views.js` using `multi_replace_file_content` to add the fourth card layout to the home view, add its entry row to the work view, register its sandbox initializer, write the sandbox component compiler logic, and append the case study pages for Aura AI.