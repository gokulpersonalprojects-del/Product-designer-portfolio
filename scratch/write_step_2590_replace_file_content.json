{
  "AllowMultiple": "false",
  "Description": "\"Add scroll-linked stacking animations for Yooki Case Study storyboard\"",
  "EndLine": "965",
  "Instruction": "\"Inject scroll-linked stacking layout script before the closing bracket of initProject1Sandbox.\"",
  "ReplacementContent": "\"    });\\n  });\\n\\n  // 3. Storyboard Scroll-Linked Stacking Card Animations\\n  const storyboardContainer = document.querySelector('.storyboard-stack-container');\\n  const storyboardCards = document.querySelectorAll('.storyboard-card');\\n  \\n  if (storyboardContainer && storyboardCards.length > 0) {\\n    const handleStoryboardScroll = () => {\\n      // If the storyboard container is no longer in the DOM, clean up the scroll event listener\\n      if (!document.body.contains(storyboardContainer)) {\\n        window.removeEventListener('scroll', handleStoryboardScroll);\\n        return;\\n      }\\n      \\n      storyboardCards.forEach((card, index) => {\\n        const rect = card.getBoundingClientRect();\\n        const nextCard = storyboardCards[index + 1];\\n        \\n        if (nextCard) {\\n          const nextRect = nextCard.getBoundingClientRect();\\n          const nextStickyTop = 80 + (index + 1) * 16;\\n          \\n          // The next card starts sliding over this card when the next card's top is less than this card's bottom.\\n          const overlapStart = rect.bottom;\\n          const overlapEnd = nextStickyTop;\\n          const totalOverlapDistance = overlapStart - overlapEnd;\\n          \\n          if (totalOverlapDistance > 0) {\\n            const currentDistance = nextRect.top - overlapEnd;\\n            const progress = 1 - Math.max(0, Math.min(1, currentDistance / totalOverlapDistance));\\n            \\n            // Progress goes from 0 (no overlap) to 1 (fully covered by next card)\\n            const scale = 1 - (progress * 0.04); // subtle scale down to 0.96\\n            const opacity = 1 - (progress * 0.35); // fade opacity down to 0.65\\n            const blur = progress * 1.5; // subtle blur\\n            const brightness = 1 - (progress * 0.15); // dim slightly\\n            \\n            card.style.transform = `scale(${scale})`;\\n            card.style.opacity = `${opacity}`;\\n            card.style.filter = `blur(${blur}px) brightness(${brightness})`;\\n          } else {\\n            card.style.tra\n<truncated 490 bytes>",
  "StartLine": "963",
  "TargetContent": "\"    });\\n  });\\n}\"",
  "TargetFile": "\"c:\\\\Users\\\\ASUS\\\\Desktop\\\\new portfolio\\\\views.js\"",
  "toolAction": "\"Updating views.js controller\"",
  "toolSummary": "\"Edit views.js controller\""
}